import 'package:flutter/material.dart';

class FishBagScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('FishBag')),
      body: Center(child: Text('Bienvenue sur FishBagScreen')),
    );
  }
}