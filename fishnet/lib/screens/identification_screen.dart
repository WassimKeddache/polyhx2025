import 'package:flutter/material.dart';

class IdentificationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('IdentificationScreen')),
      body: Center(child: Text('Bienvenue sur IdentificationScreen')),
    );
  }
}